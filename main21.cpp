#include <iostream>
#include <cmath>
using namespace std;
int main(){
double a,b;
cin>>a>>b;
cout<<"Area = "<<(a*b)/2<<", Perimeter = "<<pow(pow(a,2)+pow(b,2),0.5)+a+b<<", Hypothenuse = "<<pow(pow(a,2)+pow(b,2),0.5);
}

